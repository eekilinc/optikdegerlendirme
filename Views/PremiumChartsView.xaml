<UserControl x:Class="OptikFormApp.Views.PremiumChartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    
    <UserControl.Resources>
        <!-- Chart Animasyonları -->
        <Storyboard x:Key="ChartFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                           From="0" To="1" Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </UserControl.Resources>
    
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="20">
                
                <!-- Soru Başarı Oranları - Bar Chart -->
                <Border Background="{DynamicResource CardBg}" CornerRadius="12" Padding="20" Margin="0,0,0,25">
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard Storyboard="{StaticResource ChartFadeIn}"/>
                        </EventTrigger>
                    </Border.Triggers>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="300"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="📊 Soru Başarı Oranları" 
                                  FontSize="18" FontWeight="Bold" 
                                  Foreground="{DynamicResource TextMain}" Margin="0,0,0,15"/>
                        
                        <Border Grid.Row="1" Name="QuestionSuccessChart" 
                                   Background="Transparent" Cursor="Hand"/>
                    </Grid>
                </Border>

                <!-- Puan Dağılımı - Line Chart -->
                <Border Background="{DynamicResource CardBg}" CornerRadius="12" Padding="20" Margin="0,0,0,25">
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard Storyboard="{StaticResource ChartFadeIn}"/>
                        </EventTrigger>
                    </Border.Triggers>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="250"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="📈 Puan Dağılımı" 
                                  FontSize="18" FontWeight="Bold" 
                                  Foreground="{DynamicResource TextMain}" Margin="0,0,0,15"/>
                        
                        <Border Grid.Row="1" Name="ScoreDistributionChart" 
                                   Background="Transparent" Cursor="Hand"/>
                    </Grid>
                </Border>

                <!-- Kitapçık Performansı - Pie Chart -->
                <Border Background="{DynamicResource CardBg}" CornerRadius="12" Padding="20" Margin="0,0,0,25">
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard Storyboard="{StaticResource ChartFadeIn}"/>
                        </EventTrigger>
                    </Border.Triggers>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="300"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="🎯 Kitapçık Performansı" 
                                      FontSize="18" FontWeight="Bold" 
                                      Foreground="{DynamicResource TextMain}" Margin="0,0,0,15"/>
                            
                            <Border Name="BookletPerformanceChart" 
                                       Background="Transparent" Height="200" Cursor="Hand"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Column="1" Margin="20,0,0,0">
                            <TextBlock Text="📊 İstatistikler" 
                                      FontSize="16" FontWeight="Bold" 
                                      Foreground="{DynamicResource TextMain}" Margin="0,0,0,15"/>
                            
                            <Border Background="#10B98120" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                <StackPanel>
                                    <TextBlock Text="A Kitapçığı" FontSize="12" 
                                              Foreground="{DynamicResource TextMuted}"/>
                                    <TextBlock Text="68.2%" FontSize="24" FontWeight="Bold" 
                                              Foreground="#10B981"/>
                                </StackPanel>
                            </Border>
                            
                            <Border Background="#F59E0B20" CornerRadius="8" Padding="15">
                                <StackPanel>
                                    <TextBlock Text="B Kitapçığı" FontSize="12" 
                                              Foreground="{DynamicResource TextMuted}"/>
                                    <TextBlock Text="72.4%" FontSize="24" FontWeight="Bold" 
                                              Foreground="#F59E0B"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Zaman Çizelgesi - Line Chart -->
                <Border Background="{DynamicResource CardBg}" CornerRadius="12" Padding="20">
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard Storyboard="{StaticResource ChartFadeIn}"/>
                        </EventTrigger>
                    </Border.Triggers>
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="250"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="⏱️ Zaman Analizi" 
                                  FontSize="18" FontWeight="Bold" 
                                  Foreground="{DynamicResource TextMain}" Margin="0,0,0,15"/>
                        
                        <Border Grid.Row="1" Name="TimeAnalysisChart" 
                                   Background="Transparent" Cursor="Hand"/>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
