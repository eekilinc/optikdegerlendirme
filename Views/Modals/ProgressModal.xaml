<UserControl x:Class="OptikFormApp.Views.Modals.ProgressModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="500">
    <Grid Background="#80000000" 
          Visibility="{Binding Progress.IsBusy, Converter={StaticResource BoolToVis}}"
          Panel.ZIndex="2000">
        
        <Border Width="450" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                CornerRadius="12"
                Padding="30">
            
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                  Opacity="0.2" 
                                  BlurRadius="20" 
                                  ShadowDepth="5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Title -->
                <TextBlock Grid.Row="0" 
                          Text="{Binding Progress.OperationTitle}" 
                          FontSize="18" 
                          FontWeight="Bold" 
                          Foreground="#1F2937"
                          Margin="0,0,0,8"/>
                
                <!-- Status Message -->
                <TextBlock Grid.Row="1" 
                          Text="{Binding Progress.StatusMessage}" 
                          FontSize="14" 
                          Foreground="#6B7280"
                          TextTrimming="CharacterEllipsis"
                          Margin="0,0,0,20"/>
                
                <!-- Progress Bar -->
                <Grid Grid.Row="2" Margin="0,0,0,20">
                    <ProgressBar Height="8" 
                                Value="{Binding Progress.CurrentProgress, Mode=OneWay}" 
                                Maximum="{Binding Progress.MaximumProgress, Mode=OneWay}"
                                Background="#E5E7EB"
                                Foreground="#3B82F6"
                                BorderThickness="0">
                        <ProgressBar.Style>
                            <Style TargetType="ProgressBar">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ProgressBar">
                                            <Border Background="{TemplateBinding Background}" 
                                                   CornerRadius="4">
                                                <Grid>
                                                    <Border x:Name="PART_Track" 
                                                           CornerRadius="4"/>
                                                    <Border x:Name="PART_Indicator" 
                                                           Background="{TemplateBinding Foreground}" 
                                                           HorizontalAlignment="Left"
                                                           CornerRadius="4">
                                                        <Border.Width>
                                                            <MultiBinding Converter="{StaticResource MultiValuePercentageConverter}">
                                                                <Binding Path="Value" RelativeSource="{RelativeSource TemplatedParent}"/>
                                                                <Binding Path="Maximum" RelativeSource="{RelativeSource TemplatedParent}"/>
                                                                <Binding Path="ActualWidth" ElementName="PART_Track"/>
                                                            </MultiBinding>
                                                        </Border.Width>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ProgressBar.Style>
                    </ProgressBar>
                </Grid>
                
                <!-- Percentage and Cancel Button -->
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                              Text="{Binding Progress.ProgressPercentage, StringFormat={}{0:F0}%}" 
                              FontSize="14" 
                              FontWeight="SemiBold"
                              Foreground="#3B82F6"
                              VerticalAlignment="Center"/>
                    
                    <Button Grid.Column="1" 
                           Content="İptal" 
                           Command="{Binding CancelProgressCommand}"
                           Visibility="{Binding Progress.CanCancel, Converter={StaticResource BoolToVis}}"
                           Style="{StaticResource OutlineButton}"
                           Padding="20,8"
                           FontSize="13"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
