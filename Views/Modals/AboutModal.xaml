<UserControl x:Class="OptikFormApp.Views.Modals.AboutModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="450">
    <Grid Visibility="{Binding IsAboutOpen, Converter={StaticResource BoolToVis}}" Background="{DynamicResource ModalBackdrop}">
        <Border Background="{DynamicResource CardBg}" CornerRadius="20" Width="420" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="40,35">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="40" ShadowDepth="12"/>
            </Border.Effect>
            <StackPanel HorizontalAlignment="Center">
                <Border Width="100" Height="100" CornerRadius="20" Background="{DynamicResource AltRowBg}" Margin="0,0,0,25" Padding="20">
                    <Image Source="/Assets/app_logo.png" Stretch="Uniform"/>
                </Border>
                
                <TextBlock Text="Optik Değerlendirme" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource TextMain}" HorizontalAlignment="Center"/>
                <TextBlock Text="Versiyon 2.1.0" FontSize="13" Foreground="{DynamicResource TextMuted}" Margin="0,5,0,25" HorizontalAlignment="Center"/>
                
                <TextBlock Text="Bu uygulama AĞLASUN MYO sınav değerlendirme süreçlerini dijitalleştirmek ve hızlandırmak amacıyla geliştirilmiştir." 
                           FontSize="14" Foreground="{DynamicResource TextMain}" TextAlignment="Center" TextWrapping="Wrap" LineHeight="22" Margin="0,0,0,30"/>
                
                <Border Background="{DynamicResource AltRowBg}" CornerRadius="10" Padding="15,12" Margin="0,0,0,30">
                    <StackPanel>
                        <TextBlock Text="Geliştirici" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Text="Ekrem KILINÇ" FontSize="15" FontWeight="SemiBold" Foreground="#3B82F6" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Button Content="Kapat" Command="{Binding CloseAboutCommand}" 
                        Background="#3B82F6" Foreground="White" Padding="40,10" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
