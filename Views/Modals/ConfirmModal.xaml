<UserControl x:Class="OptikFormApp.Views.Modals.ConfirmModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="420">
    <Grid Visibility="{Binding IsConfirmModalOpen, Converter={StaticResource BoolToVis}}" Background="{DynamicResource ModalBackdrop}">
        <Border Background="{DynamicResource CardBg}" CornerRadius="12" Width="420" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="25" ShadowDepth="5"/>
            </Border.Effect>
            <StackPanel>
                <!-- Icon -->
                <TextBlock Text="&#xE7BA;" FontFamily="Segoe UI Symbol" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,15" Foreground="#F59E0B"/>
                
                <!-- Title -->
                <TextBlock Text="{Binding ConfirmModalTitle}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextMain}" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                
                <!-- Message -->
                <TextBlock Text="{Binding ConfirmModalMessage}" FontSize="14" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,25" TextWrapping="Wrap" TextAlignment="Center" LineHeight="22"/>
                
                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="İptal" Command="{Binding CancelConfirmCommand}" Style="{StaticResource OutlineButton}" Margin="0,0,5,0"/>
                    <Button Grid.Column="1" Content="{Binding ConfirmButtonText}" Command="{Binding ConfirmActionCommand}" Background="#EF4444" Margin="5,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
