<UserControl x:Class="OptikFormApp.Views.WorkflowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="1200">
    <Border Background="{DynamicResource CardBg}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="15" ShadowDepth="2"/>
        </Border.Effect>
        
        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- Left: Title -->
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal" Margin="0,0,25,0">
                <Border Width="40" Height="40" CornerRadius="8" Background="{DynamicResource AltRowBg}" Margin="0,0,12,0" Padding="5">
                    <Image Source="/Assets/app_logo.png" Stretch="Uniform"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="AĞLASUN MYO" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextMain}"/>
                    <TextBlock Text="Optik Değerlendirme Sistemi" FontSize="12" Foreground="{DynamicResource TextMuted}"/>
                </StackPanel>
            </StackPanel>

            <!-- Right: Actions -->
            <WrapPanel HorizontalAlignment="Left" VerticalAlignment="Center" Orientation="Horizontal">
                
                <!-- Workflow Group -->
                <Border Background="#F0FDF4" CornerRadius="6" Padding="8,6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="▶️ İŞLEMLER" FontSize="10" FontWeight="Bold" Foreground="#166534" VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.8"/>
                        <Button Command="{Binding LoadTxtCommand}" Style="{StaticResource PipelineButton}" Background="#16A34A" BorderBrush="#15803D" Foreground="White" Padding="10,6" Margin="0,0,6,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📁" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="Yükle" FontSize="12" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding OpenSaveExamModalCommand}" IsEnabled="{Binding Students.Count}" Padding="10,6" Margin="0,0,6,0">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource PipelineButton}">
                                    <Setter Property="Background" Value="#22C55E"/>
                                    <Setter Property="BorderBrush" Value="#16A34A"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasUnsavedData}" Value="True">
                                            <Setter Property="Background" Value="#DC2626"/>
                                            <Setter Property="BorderBrush" Value="#B91C1C"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="12" Margin="0,0,4,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="✓"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasUnsavedData}" Value="True">
                                                    <Setter Property="Text" Value="!"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock FontSize="12" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Kaydedildi"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding HasUnsavedData}" Value="True">
                                                    <Setter Property="Text" Value="Kaydet"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding EvaluateCommand}" Style="{StaticResource PipelineButton}" Background="#7C3AED" BorderBrush="#6D28D9" Foreground="White" Padding="10,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚡" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="Hesapla" FontSize="12" FontWeight="Bold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
                
                <!-- Config Group -->
                <Border Background="{DynamicResource AltRowBg}" CornerRadius="6" Padding="8,6" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙️ AYARLAR" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.7"/>
                        <Button Command="{Binding OpenModalCommand}" Style="{StaticResource OutlineButton}" Padding="8,6" Margin="0,0,4,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" Margin="0,0,4,0"/>
                                <TextBlock Text="Cevap Anahtarı" FontSize="12"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding OpenGeneralConfigCommand}" Style="{StaticResource OutlineButton}" Padding="8,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" Margin="0,0,4,0"/>
                                <TextBlock Text="Sınav Ayarları" FontSize="12"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
                
                <!-- Export Group -->
                <Border Background="#FEF3C7" CornerRadius="6" Padding="8,6" Visibility="{Binding Students.Count, Converter={StaticResource DataToVisConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📤 DIŞA AKTAR" FontSize="10" FontWeight="Bold" Foreground="#92400E" VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.8"/>
                        <Button Command="{Binding ExportExcelCommand}" Style="{StaticResource PipelineButton}" Background="#D97706" BorderBrush="#B45309" Foreground="White" Padding="10,6" Margin="0,0,6,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="Excel" FontSize="12"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding ExportCsvCommand}" Style="{StaticResource PipelineButton}" Background="#C2410C" BorderBrush="#9A3412" Foreground="White" Padding="10,6" Margin="0,0,6,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📋" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="CSV" FontSize="12"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding ExportFullReportCommand}" Style="{StaticResource PipelineButton}" Background="#BE185D" BorderBrush="#9D174D" Foreground="White" Padding="10,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📄" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="PDF Rapor" FontSize="12"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </WrapPanel>
    </Border>
</UserControl>
