<UserControl x:Class="OptikFormApp.Views.ToastNotificationHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    
    <UserControl.Resources>
        <!-- Percentage to Width Converter -->
        <views:PercentageToWidthConverter x:Key="PercentageToWidthConverter" xmlns:views="clr-namespace:OptikFormApp.Views"/>
        
        <!-- Close Button Style - MUST be defined before ToastTemplate -->
        <Style x:Key="ToastCloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="14"
                                Opacity="0.5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Toast Item Template -->
        <DataTemplate x:Key="ToastTemplate">
            <Border Background="{Binding BackgroundBrush}" 
                    CornerRadius="12" 
                    Padding="16,12" 
                    Margin="0,6,12,0"
                    MinWidth="320"
                    MaxWidth="400"
                    RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Border.RenderTransform>
                <Border.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                                 From="0.8" To="1" Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                                 From="0.8" To="1" Duration="0:0:0.25">
                                    <DoubleAnimation.EasingFunction>
                                        <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                                 From="0" To="1" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Border.Triggers>
                <Border.Effect>
                    <DropShadowEffect Color="#000000" 
                                      Opacity="0.15" 
                                      BlurRadius="20" 
                                      ShadowDepth="8"
                                      Direction="270"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="3"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Icon -->
                        <Border Grid.Column="0" 
                                Width="36" 
                                Height="36" 
                                Background="White" 
                                CornerRadius="18"
                                Margin="0,0,12,0">
                            <Path Data="{Binding IconPath}" 
                                  Fill="{Binding IconBrush}" 
                                  Stretch="Uniform"
                                  Width="20"
                                  Height="20"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                        </Border>
                        
                        <!-- Content -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Title}" 
                                       FontWeight="SemiBold" 
                                       FontSize="14"
                                       Foreground="White"/>
                            <TextBlock Text="{Binding Message}" 
                                       FontSize="13"
                                       Foreground="#E0E0E0"
                                       TextWrapping="Wrap"
                                       Margin="0,2,0,0"/>
                        </StackPanel>
                        
                        <!-- Close Button -->
                        <Button Grid.Column="2" 
                                Command="{Binding DataContext.DismissNotificationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding Id}"
                                Style="{StaticResource ToastCloseButton}"
                                Width="28"
                                Height="28"
                                Margin="8,0,0,0"
                                VerticalAlignment="Top">
                            <Path Data="M6 18L18 6M6 6l12 12" 
                                  Stroke="White" 
                                  StrokeThickness="2"
                                  Stretch="Uniform"
                                  Width="14"
                                  Height="14"/>
                        </Button>
                    </Grid>
                    
                    <!-- Progress Bar -->
                    <Border Grid.Row="1" 
                            Background="White" 
                            Opacity="0.3"
                            Height="3"
                            CornerRadius="1.5"
                            VerticalAlignment="Bottom"
                            Margin="0,8,0,0">
                        <Border Width="{Binding Progress, Converter={StaticResource PercentageToWidthConverter}, ConverterParameter=360}"
                                Background="White"
                                HorizontalAlignment="Left"
                                CornerRadius="1.5"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    
    <!-- Notification Container -->
    <ItemsControl ItemsSource="{Binding}" 
                  ItemTemplate="{StaticResource ToastTemplate}"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Margin="0,60,0,0">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <StackPanel Orientation="Vertical"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
    </ItemsControl>
</UserControl>
