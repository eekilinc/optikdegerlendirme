<UserControl x:Class="OptikFormApp.Views.Modals.LearningOutcomesModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="650">
    <Grid Visibility="{Binding IsLearningOutcomesOpen, Converter={StaticResource BoolToVis}}" Background="{DynamicResource ModalBackdrop}">
        <Border Background="{DynamicResource CardBg}" CornerRadius="16" Width="650" MaxHeight="700" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="30">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.15" BlurRadius="30" ShadowDepth="10"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,15">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="🎯 Konu / Kazanım Yönetimi" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextMain}"/>
                        <TextBlock Text="Soru numaralarına göre konu başlıklarını tanımlayın." FontSize="12" Foreground="{DynamicResource TextMuted}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Content="✕" Command="{Binding CloseLearningOutcomesCommand}" DockPanel.Dock="Right" 
                            Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextMuted}" FontSize="18" Padding="10,5"/>
                </DockPanel>

                <Border Grid.Row="1" Background="{DynamicResource AltRowBg}" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,15,0">
                            <TextBlock Text="Kazanım Adı" FontSize="11" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding NewOutcomeName, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="{DynamicResource CardBg}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,15,0">
                            <TextBlock Text="Kitapçık" FontSize="11" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                            <ComboBox SelectedItem="{Binding NewOutcomeBooklet}" Padding="8" Background="{DynamicResource CardBg}">
                                <ComboBoxItem>A</ComboBoxItem>
                                <ComboBoxItem>B</ComboBoxItem>
                                <ComboBoxItem>C</ComboBoxItem>
                                <ComboBoxItem>D</ComboBoxItem>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,15,0">
                            <TextBlock Text="Soru No (Örn: 1-5)" FontSize="11" Foreground="{DynamicResource TextMuted}" Margin="0,0,0,5"/>
                            <TextBox Text="{Binding NewOutcomeRange, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="{DynamicResource CardBg}"/>
                        </StackPanel>
                        <Button Grid.Column="3" Content="Ekle" Command="{Binding AddLearningOutcomeCommand}" 
                                VerticalAlignment="Bottom" Padding="20,8" Background="#3B82F6" Foreground="White"/>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="2" ItemsSource="{Binding LearningOutcomes}" AutoGenerateColumns="False" CanUserAddRows="False"
                          Background="Transparent" BorderThickness="0" RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Konu Başlığı" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Kitapçık" Binding="{Binding BookletName}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Soru Aralığı" Binding="{Binding QuestionNumbersRaw}" Width="120" IsReadOnly="True"/>
                        <DataGridTemplateColumn Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Sil" Command="{Binding DataContext.RemoveLearningOutcomeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}" Background="Transparent" Foreground="#EF4444" BorderBrush="#EF4444" BorderThickness="1" Padding="10,2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Kapat" Command="{Binding CloseLearningOutcomesCommand}" Background="#10B981" Padding="25,10"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
